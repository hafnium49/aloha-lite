[project]
name = "so101-mcp-server"
version = "1.0.0"
description = "SO-101 Robot Control MCP Server for ALOHA-Lite"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]==1.10.0",
    "requests>=2.31.0",
    "pillow>=10.0.0",
]

[tool.uv]
dev-dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
